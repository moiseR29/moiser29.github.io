---
/*
  Componente reutilizable para cada proyecto.
  TODO: Cambiá los valores de ejemplo (title, description, image, link)
*/
const { title, description, images, link } = Astro.props;
---

<div class="bg-gray-100 dark:bg-gray-800 rounded-xl shadow-md overflow-hidden flex flex-col items-center">
  <div class="p-5 flex flex-col flex-grow text-center">
    <h4 class="text-lg font-semibold mb-2">{title}</h4>
    <p class="text-gray-600 dark:text-gray-400 text-sm mb-3">
      {description}
    </p>
    <a href={link} target="_black" class="text-indigo-500 hover:underline text-sm font-medium">Ver en GitHub →</a>
  </div>
</div>
