---
/*
  Componente reutilizable para cada proyecto.
  TODO: Cambiá los valores de ejemplo (title, description, image, link)
*/
const { title, description, images, link } = Astro.props;
---

<div class="bg-slate-800 rounded-lg p-4 shadow-lg transition hover:scale-105">
  <h3 class="text-xl font-semibold mb-2">{title}</h3>
  <p class="text-gray-300 mb-4">{description}</p>

  <!-- Carrusel básico -->
<!--   
  <div class="relative w-full overflow-hidden rounded-lg">
    {images.map((img, i) => (
      <img src={img} alt={title} class="w-full object-cover" loading="lazy" />
    ))}
  </div> -->

  <a href={link} target="_blank" class="inline-block mt-4 text-blue-400 hover:text-blue-300">
    Ver repositorio →
  </a>
</div>
